pub enum AppEvent {
    Quit,
    MoveDown(usize),
    MoveUp(usize),
    GoTop,
    GoBottom,
    GoMiddle,
    Enter,
    Back,
    JumpToPlaylists,
    JumpToLiked,
    JumpToArtists,
    EnterGMode,
    ExitGMode,
}
